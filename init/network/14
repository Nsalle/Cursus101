# 14. What file contains the local DNS entries?

The file that contains the local DNS entries is in '/etc/hosts'.
We can try running the following command:

"cat /etc/hosts"

to see:

127.0.0.1       localhost
255.255.255.255 broadcasthost
::1             localhost