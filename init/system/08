# 8. What file contains the RSA keys of systems that are authorized to connect via SSH?

Generate a rsa key by using:
ssh-keygen -t rsa

You can choose a file for the key but by default you will have it in:
/home/nsalle/.ssh/id_rsa

And the authorized key:
/home/nsalle/.ssh/id_rsa.pub